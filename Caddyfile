# TODO: Replace :80 with your actual domain before deploying (e.g. budget.mysite.com)
:80 {
    handle /api/* {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:80
    }
}
